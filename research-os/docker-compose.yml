version: '3.9'
services:
  web:
    build: ./frontend-next
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - ai
  ai:
    build: ./ai_scientist
    ports:
      - "8000:8000"
    env_file:
      - .env
